$(document).ready(function() {

  $( "#fatura" ).on('click', function() {
    window.location.href = 'http://sgp.logtel.net.br:8000/accounts/central/login';
  });

  $( "#suporte-remoto" ).on('click', function() {
    window.location.href = 'https://download.anydesk.com/AnyDesk.exe?_ga=2.192795882.1370282749.1501879137-1731539336.1501879137';
  });

  $( "#teste-velocidade" ).on('click', function() {
    window.location.href = 'https://www.minhaconexao.com.br/';
  });

  $( "#login_area_cliente" ).on('click', function() {
    window.location.href = 'http://sgp.logtel.net.br:8000/accounts/central/login';
  });
});


  $( ".modal_watch" ).hover(
    function() {
      $( this ).modal("#modal_watch");
    }, function() {
      $( this ).find( "span" ).last().remove();
    }
  );
   
  $( "li.fade" ).hover(function() {
    $( this ).fadeOut( 100 );
    $( this ).fadeIn( 500 );
  });



  
/* 
  var smartLoaded = true;

  while(smartLoaded) {
    if(typeof $("#smartzap_chat_btn_iframe").css("margin") !== 'undefined') {
      $("#smartzap_chat_btn_iframe").css("margin", "30px");
      smartLoaded = false;
    } else {
      //console.log($("#smartzap_chat_btn_iframe").css("margin"));
    }
  } */


$('.barra-conversao').css({
  'position':'relative',
  'display':'none'
});
  
$(window).on('scroll', function() {
  var height = $(window).scrollTop();
  if(height > 450) {
    $('.barra-conversao').css({
      'position':'fixed'
    });
    $('.barra-conversao').fadeIn('slow');
  } else {
    $('.barra-conversao').fadeOut('slow', function(){
      $('.barra-conversao').css({'position':'relative'});
    });
  }
});

function limpa_formulário_cep() {
    // Limpa valores do formulário de cep.
    $("#rua").val("");
    $("#bairro").val("");
    $("#cidade").val("");
    $("#uf").val("");
}

//Quando o campo cep perde o foco.
$("#cep").on('blur', function() {

    //Nova variável "cep" somente com dígitos.
    var cep = $(this).val().replace(/\D/g, '');

    //Verifica se campo cep possui valor informado.
    if (cep != "") {

        //Expressão regular para validar o CEP.
        var validacep = /^[0-9]{8}$/;

        //Valida o formato do CEP.
        if(validacep.test(cep)) {

            //Preenche os campos com "..." enquanto consulta webservice.
            $("#rua").val("...");
            $("#bairro").val("...");
            $("#cidade").val("...");
            $("#uf").val("...");

            //Consulta o webservice viacep.com.br/
            $.getJSON("https://viacep.com.br/ws/"+ cep +"/json/?callback=?", function(dados) {

                if (!("erro" in dados)) {
                    //Atualiza os campos com os valores da consulta.
                    $("#rua").val(dados.logradouro);
                    $("#bairro").val(dados.bairro);
                    $("#cidade").val(dados.localidade);
                    $("#uf").val(dados.uf);
                } //end if.
                else {
                    //CEP pesquisado não foi encontrado.
                    limpa_formulário_cep();
                    console.log("CEP não encontrado.");
                }
            });
        } //end if.
        else {
            //cep é inválido.
            limpa_formulário_cep();
            console.log("Formato de CEP inválido.");
        }
    } //end if.
    else {
        //cep sem valor, limpa formulário.
        limpa_formulário_cep();
    }
});

